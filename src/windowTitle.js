// TODO: Register $TM_* variables as window title variables using `registerWindowTitleVariable`
// (https://github.com/microsoft/vscode/pull/225408, available since VS Code 1.93.0).
// Users can then reference them in their window.title setting as ${windowTitleVariable.TM_*}.

const activate = (_context) => {}

module.exports = {
  activate,
}
